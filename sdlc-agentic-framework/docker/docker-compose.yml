services:
  backend:
    build: {context: .., dockerfile: docker/Dockerfile.backend}
    ports: ["8000:8000"]
    volumes: ["../sample-output:/app/sample-output"]
  frontend:
    build: {context: .., dockerfile: docker/Dockerfile.frontend}
    ports: ["3000:5173"]
    depends_on: [backend]
