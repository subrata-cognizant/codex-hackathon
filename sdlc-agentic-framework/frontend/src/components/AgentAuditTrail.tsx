type Event={sequence:number;agent:string;status:string;timestamp:string;duration_ms:number};
export function AgentAuditTrail({events=[]}:{events?:Event[]}){return <section className="panel"><div className="section-title"><div><small>EXECUTION EVIDENCE</small><h2>Agent audit trail</h2></div><span className="pill">{events.length} events</span></div>{events.length===0?<p className="muted">Run the workflow to capture timestamped agent activity.</p>:<div className="audit">{events.map(e=><div className="audit-row" key={`${e.sequence}-${e.agent}`}><span className="audit-icon">✓</span><div><b>{e.agent}</b><small>{new Date(e.timestamp).toLocaleTimeString()} · {e.duration_ms} ms</small></div><span className="success">{e.status}</span></div>)}</div>}</section>}
