export function ApprovalStatus({gates}:{gates?:Record<string,string>}){return <section className="panel"><div className="section-title"><div><small>HUMAN-IN-THE-LOOP</small><h2>Approval governance</h2></div></div><div className="gate-grid">{Object.entries(gates||{brd:'pending',code_plan:'locked'}).map(([name,state])=><article key={name}><span className={`gate-dot ${state}`}/><div><b>{name==='brd'?'Business Requirements':'Implementation Plan'}</b><small>{state.replace('_',' ')}</small></div></article>)}</div></section>}
