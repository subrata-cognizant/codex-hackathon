const icons:Record<string,string>={json:'{}',md:'¶',py:'Py',tsx:'Ts'};
export function ArtifactGallery({items,onSelect}:{items:string[],onSelect:(name:string)=>void}){return <section className="panel"><div className="section-title"><div><small>DELIVERY PACKAGE</small><h2>Generated artifacts</h2></div><span className="pill">{items.length} files</span></div><div className="artifact-grid">{items.map(name=>{const ext=name.split('.').pop()||'';return <button className="artifact-card" onClick={()=>onSelect(name)} key={name}><i>{icons[ext]||'↗'}</i><span><b>{name.split('/').pop()}</b><small>{name.includes('generated_code')?'Source code':'Traceable artifact'}</small></span><em>View →</em></button>})}</div></section>}
