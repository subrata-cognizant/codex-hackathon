type Graph={nodes:{id:string;type:string;label:string}[];edges:{from:string;to:string;relationship:string}[]};
export function TraceabilityGraph({graph}:{graph?:Graph}){if(!graph)return <p className="muted">Complete the workflow and load lineage to see requirement-to-release evidence.</p>;return <div className="lineage" role="img" aria-label="Requirement to release lineage">{graph.nodes.map((node,index)=><div className="lineage-item" key={node.id}><div className={`node type-${node.type.toLowerCase().replaceAll(' ','-')}`}><small>{node.type}</small><b>{node.id}</b><span>{node.label}</span></div>{index<graph.nodes.length-1&&<div className="edge"><span>{graph.edges[index]?.relationship}</span>→</div>}</div>)}</div>}
